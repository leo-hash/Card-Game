# MyCardGame
small project to try to implement multiple card games with focus on reusability
and clean design

## Installation
```bash
pip install -r requirements.txt
pip install -e .
```

## Tests
python -m 